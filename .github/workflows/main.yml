name: Workflow Testing

on: 
  workflow_dispatch: 
  push: 
    branches: ["main"]
  pull_request: 
    branches: ["main"]

jobs: 
  experimental-great:
    runs-on: ubuntu-latest
    steps: 
      - name: Greet the user
        run: echo "Hello world"
    
